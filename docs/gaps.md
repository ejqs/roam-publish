# Gaps (intentional)

Dropped vs earlier prototype UX:

- Overlay badges / chips / HUD
- Share popover (visibility/scope UI)
- Outdated scanner
- Published-items dialog
- Teams/groups

Still need a real Roam append-only token for Connect in production (`ROAM_TOKEN_VALIDATION` must not be `skip`).

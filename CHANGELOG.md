# Changelog

## Unreleased

- Real HTTP client against roam-publish-web (token exchange + publish CRUD)
- Settings: Roam temporary token → Connect → store API key; server URL; disconnect
- Removed teams/groups from UX and data model
- Content serialized as `roam-json-v1`; public URL shape `/{graph}/{uid}`
- Default visibility: unlisted
